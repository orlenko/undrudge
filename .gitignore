.venv/
.uv-cache/
.test-cfg/
.test-data/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.DS_Store

# The real dispatch prototype config + plist name sensitive internal
# projects (and a live legal matter) and machine paths — never commit
# them to this public repo. A sanitized config.example.json is vendored
# instead. The live config lives at ~/.config/undrudge-dispatch/.
prototype/config.json
prototype/undrudge.dispatch.plist

# Per-machine a2a/rationale messaging command copies — not undrudge
# project files. Live locally only.
.claude/commands/rat/
